# Disposable local PostgreSQL only; never reuse application credentials or data.
TEST_DATABASE_URL=postgresql://ticketing_test:local-test-only@127.0.0.1:55432/ticketing_test
JWT_SECRET=local-test-only-jwt-secret
NODE_ENV=test
